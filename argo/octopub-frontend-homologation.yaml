apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: octopub-homologation
  namespace: argocd
  annotations:
   argo.octopus.com/project: "octopub"
   argo.octopus.com/environment: "homologation"
spec:
  project: default
  source:
    repoURL: https://github.com/octopussolutionsengineering/Itau-octopub.git
    targetRevision: HEAD
    path: manifests/homologation
  destination:
    server: https://kubernetes.default.svc
    namespace: homologation
  syncPolicy:
    automated: 
      prune: true
      selfHeal: true
    syncOptions:
    - Validate=false
    - CreateNamespace=true